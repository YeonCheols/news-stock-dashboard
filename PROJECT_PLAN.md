# 뉴스·주식 레이더

## 1. 한 줄 소개

최신 뉴스와 주가 변화를 한 화면에서 확인하고, 관심 종목에 영향을 줄 만한 이슈를 요약해 주는 개인용 Python 대시보드.

첫 버전은 투자 판단을 자동으로 내리는 프로그램이 아니라, 뉴스를 빠르게 읽고 기록하는 데 도움을 주는 정보 도구로 만든다.

## 2. 해결하려는 문제

- 여러 사이트를 돌아다니지 않고 관심 종목의 뉴스와 주가를 확인한다.
- 뉴스가 호재인지 악재인지 빠르게 파악한다.
- 갑자기 뉴스가 많아지거나 주가가 크게 움직인 종목을 발견한다.
- 나중에 “왜 이 종목을 샀는지” 매매 당시 근거를 되돌아본다.

## 3. 핵심 사용자 흐름

1. 관심 종목을 등록한다.
2. 프로그램이 일정 간격으로 뉴스와 주가를 수집한다.
3. 종목별 뉴스, 등락률, 거래량, 주요 키워드를 보여준다.
4. 사용자가 뉴스 원문을 읽고 중요 뉴스로 저장한다.
5. 하루 요약 화면에서 시장의 주요 흐름을 확인한다.

## 4. MVP 범위

### 반드시 포함

- 한국·미국 주식 종목 등록
- 종목별 현재가와 등락률 표시
- RSS 기반 뉴스 수집
- 뉴스 중복 제거
- 종목별 뉴스 목록과 원문 링크
- 키워드 추출
- SQLite 저장
- Streamlit 웹 대시보드
- 관심 종목 추가·삭제

### 이후 추가

- 뉴스 감성 분석
- 가격 급등락과 뉴스 발생 시점 비교
- Telegram·Discord 알림
- 매매일지
- 실적 발표·배당·금리 일정 캘린더
- 뉴스 요약 및 종목 간 연관성 분석

## 5. 추천 기술 구성

| 영역 | 선택 기술 |
|---|---|
| 언어 | Python 3.11 이상 |
| 화면 | Streamlit |
| 주가 데이터 | yfinance 또는 신뢰할 수 있는 공식·공개 API |
| 뉴스 | RSS 우선, 필요할 때 뉴스 API 추가 |
| 저장소 | SQLite + SQLAlchemy |
| 스케줄링 | APScheduler |
| 데이터 검증 | Pydantic |
| 테스트 | pytest |
| 패키지 관리 | uv 또는 venv + pip |

주가·뉴스 제공 서비스의 이용약관과 호출 제한을 확인한다. API 키는 코드에 직접 저장하지 않고 `.env`로 관리한다.

## 6. 예상 폴더 구조

```text
news-stock-radar/
├── app.py
├── pyproject.toml
├── .env.example
├── README.md
├── src/
│   ├── config.py
│   ├── database.py
│   ├── models.py
│   ├── collectors/
│   │   ├── market.py
│   │   └── news.py
│   ├── services/
│   │   ├── ranking.py
│   │   ├── sentiment.py
│   │   └── summarizer.py
│   └── ui/
│       ├── dashboard.py
│       └── components.py
└── tests/
    ├── test_news.py
    └── test_ranking.py
```

## 7. 주요 데이터 모델

### Stock

- `symbol`: 종목 코드
- `name`: 종목명
- `market`: KOSPI, NASDAQ 등
- `is_favorite`: 관심 종목 여부

### NewsArticle

- `title`: 제목
- `source`: 언론사 또는 피드명
- `url`: 원문 주소
- `published_at`: 발행 시각
- `related_symbols`: 관련 종목
- `keywords`: 주요 키워드
- `sentiment`: positive, negative, neutral
- `is_read`: 읽음 여부

### MarketSnapshot

- `symbol`
- `price`
- `change_rate`
- `volume`
- `captured_at`

## 8. 화면 설계

### 대시보드

- 상단: 마지막 수집 시각, 새로고침 버튼
- 왼쪽: 관심 종목 목록과 등락률
- 중앙: 선택 종목의 가격 카드와 최근 뉴스
- 오른쪽: 주요 키워드, 뉴스 분위기, 급변 알림
- 하단: 전체 뉴스 타임라인

### 필터

- 한국·미국 시장
- 관심 종목
- 뉴스 발행일
- 긍정·부정·중립
- 키워드 검색

## 9. 단계별 개발 일정

### 1단계 — 실행 기반

- Python 프로젝트 생성
- Streamlit 화면 띄우기
- SQLite 연결
- 샘플 종목과 샘플 뉴스 표시

완료 기준: 로컬 명령 한 번으로 대시보드가 열리고 샘플 데이터가 보인다.

### 2단계 — 실제 데이터 수집

- 주가 수집 모듈 구현
- RSS 수집 모듈 구현
- URL 기준 중복 제거
- 수집 실패와 API 제한을 사용자에게 표시

완료 기준: 관심 종목의 실제 가격과 뉴스가 저장되고 다시 실행해도 유지된다.

### 3단계 — 대시보드 완성

- 종목 추가·삭제
- 종목별 상세 화면
- 기간 및 감성 필터
- 키워드 빈도 표시
- 원문 열기

완료 기준: 매일 사용할 수 있는 개인용 뉴스 화면이 된다.

### 4단계 — 분석 기능

- 뉴스 감성 분류
- 뉴스량 급증 탐지
- 가격 변동과 뉴스 연결
- 하루 요약 생성

완료 기준: 단순 목록을 넘어 “오늘 주목할 종목”을 설명할 수 있다.

### 5단계 — 알림과 배포

- Telegram 또는 Discord 알림
- 하루 한 번 요약 발송
- Docker 또는 개인 서버 배포
- 로그와 수집 상태 페이지 추가

완료 기준: 프로그램을 계속 켜두지 않아도 정기적으로 요약을 받을 수 있다.

### 6단계 — 데스크톱 프로그램화

- Streamlit 대시보드를 브라우저 대신 독립 앱 창에서 실행
- 앱 실행 시 로컬 대시보드 서버 자동 시작
- macOS 앱 패키지와 Windows 실행 파일 생성
- 앱 아이콘과 기본 설정 화면 추가
- 데이터베이스와 로그 저장 경로를 사용자 데이터 폴더로 분리
- 앱 종료 시 로컬 서버와 백그라운드 수집 작업을 안전하게 종료
- 설치와 업데이트 방법을 README에 문서화

추천 구현 방향:

- 1차: 기존 Streamlit 화면을 유지하고 `pywebview`로 데스크톱 창에 표시
- 배포: 운영체제별 패키징 도구로 실행 파일 생성
- 이후 필요할 때만 PySide6 기반 네이티브 UI로 전환

완료 기준: 사용자가 터미널 명령 없이 앱 아이콘을 실행하고, 브라우저가 아닌 독립 창에서 뉴스·주식 대시보드를 사용할 수 있다.

## 10. 첫 실행 예시

```bash
uv venv
source .venv/bin/activate
uv pip install streamlit yfinance feedparser sqlalchemy pydantic python-dotenv pytest
streamlit run app.py
```

Windows에서는 가상환경 활성화 명령만 운영체제에 맞게 바꾼다.

## 11. 초기 설정값

`.env`에는 다음 정도만 둔다.

```text
DATABASE_URL=sqlite:///./data/radar.db
NEWS_REFRESH_MINUTES=30
DEFAULT_MARKET=US
```

외부 요약·감성 API를 붙일 때만 별도의 API 키를 추가한다.

## 12. 주의할 점

- 뉴스 감성은 참고용이며 투자 추천으로 표현하지 않는다.
- 무료 주가 데이터는 실시간이 아닐 수 있으므로 마지막 갱신 시각을 표시한다.
- 같은 뉴스의 재배포본은 URL과 제목을 기준으로 합친다.
- 수집 실패 시 빈 화면 대신 마지막 성공 시각과 오류 상태를 보여준다.
- 처음부터 모든 종목을 수집하지 말고 관심 종목 5~10개로 제한한다.

## 13. 첫 번째 목표

가장 먼저 다음 기능만 구현한다.

> 종목명 또는 티커를 입력하면 현재가, 등락률, 최근 RSS 뉴스 10개를 보여주는 Streamlit 화면

이 기능이 동작한 다음 감성 분석과 알림을 추가한다. 이 순서라면 외부 API나 AI 기능이 불안정해도 핵심 프로그램은 빠르게 완성할 수 있다.

## 14. 이 문서의 사용 목적

이 문서는 실제 구현을 담당하는 AI 에이전트(Codex)에게 프로젝트의 목적, 범위, 우선순위, 제약사항을 제공하는 기준 문서다.

Codex는 구현을 시작하기 전에 이 문서와 현재 저장소의 README를 읽고, 기존 코드가 있다면 현재 동작과 변경 범위를 먼저 파악한다. 문서와 사용자의 최신 요청이 충돌하면 최신 사용자의 명시적 요청을 우선하되, 불변조건을 깨야 하는 경우 그 이유와 영향을 먼저 설명한다.

## 15. 불변조건

다음 조건은 기능이 추가되어도 반드시 유지한다.

### 데이터와 신뢰성

- 뉴스 원문 URL과 출처를 항상 보존한다.
- 출처가 없거나 발행 시각을 확인할 수 없는 데이터는 공식 데이터처럼 표시하지 않는다.
- 외부 API 실패가 전체 화면을 중단시키지 않도록 한다.
- 마지막으로 성공한 데이터와 현재 수집 실패 상태를 구분해서 표시한다.
- 중복 뉴스가 반복해서 저장되지 않도록 안정적인 식별 기준을 둔다.
- 시간은 내부 저장 시 UTC 또는 명확한 timezone-aware datetime을 사용하고, 화면에서는 사용자 timezone으로 표시한다.
- 주가 데이터가 지연·장외·비공식 데이터일 수 있다는 점을 화면에 표시한다.

### 사용자 안전

- 매수·매도·수익을 보장하거나 특정 행동을 직접 권하는 문구를 사용하지 않는다.
- 감성 분석과 요약은 참고 정보로 표시한다.
- API 키, 계좌 정보, 개인 정보, 로컬 비밀값을 로그나 Git에 남기지 않는다.
- 사용자가 원문을 확인할 수 있도록 모든 요약 뉴스에 원문 링크를 제공한다.

### 제품 범위

- 첫 버전은 개인용 정보 대시보드이며 자동 주문 기능을 포함하지 않는다.
- 초기 수집 대상은 사용자가 등록한 관심 종목으로 제한한다.
- 외부 AI 기능이 없어도 기본 뉴스 목록과 주가 화면은 동작해야 한다.
- 데스크톱 환경에서 한 명이 로컬 실행하는 것을 MVP의 기본 배포 환경으로 한다.

### 코드 품질

- 수집, 저장, 분석, UI 코드를 분리한다.
- API 호출을 UI 렌더링 코드에 직접 섞지 않는다.
- 기능 추가 시 관련 테스트를 함께 추가한다.
- 설정값은 환경변수 또는 설정 파일로 관리하고 코드에 하드코딩하지 않는다.
- 실행 방법과 필요한 환경변경 사항은 README에 반영한다.

## 16. Codex 구현 작업 규칙

Codex는 다음 순서로 작업한다.

1. 현재 폴더와 기존 파일을 확인한다.
2. 이 문서의 MVP 범위와 불변조건을 기준으로 작은 단위의 구현 계획을 세운다.
3. 한 번에 하나의 기능만 구현한다.
4. 실제 API를 연결하기 전에 샘플 또는 고정 fixture로 기본 화면을 검증한다.
5. 수집 실패, 빈 결과, 잘못된 종목 코드, 중복 데이터, API 응답 지연을 테스트한다.
6. 변경 후 린트·테스트·실행 확인을 수행한다.
7. 완료 보고에는 변경 파일, 실행 명령, 테스트 결과, 남은 한계를 포함한다.

사용자가 명시하지 않은 대규모 기술 변경, 유료 API 도입, 자동 주문 기능, 인증·결제 기능은 임의로 추가하지 않는다.

## 17. 후속 플랜

### Phase 1 — 작동하는 수집기

- 관심 종목을 코드 또는 간단한 UI로 등록
- 주가 1회 조회
- RSS 뉴스 1개 이상 조회
- SQLite에 저장
- 수집 결과를 화면에 표시

산출물: 실제로 실행 가능한 최소 대시보드.

### Phase 2 — 신뢰할 수 있는 데이터 파이프라인

- 수집 주기 설정
- 재시도와 timeout 추가
- 소스별 수집 상태 기록
- 중복 제거 및 오래된 데이터 정리 정책 추가
- 데이터 수집 시각과 실패 원인 표시

산출물: 며칠 동안 실행해도 데이터가 망가지지 않는 수집 구조.

### Phase 3 — 읽기 경험 개선

- 종목별 뉴스 탭
- 전체 뉴스 검색
- 기간·시장·출처 필터
- 읽음·중요 표시
- 키워드 빈도 및 관련 종목 표시

산출물: 매일 사용할 수 있는 개인 뉴스 리더.

### Phase 4 — 분석 보조

- 한국어·영어 뉴스 감성 분류
- 제목 기반 주제 분류
- 뉴스량 급증 감지
- 가격 변동과 뉴스 이벤트 타임라인
- 요약 생성 전후 원문 비교

산출물: 오늘 확인할 이슈를 우선순위로 보여주는 화면.

### Phase 5 — 개인화와 알림

- 종목별 키워드 알림
- 등락률 임계값 알림
- Telegram 또는 Discord 전송
- 아침 요약과 장 마감 요약
- 중요 뉴스에 대한 개인 메모

산출물: 프로그램을 열지 않아도 핵심 변화가 전달되는 시스템.

### Phase 6 — 배포와 운영

- Docker 실행 환경
- 스케줄러를 포함한 서버 실행 모드
- 환경변수 검증
- 수집 건강 상태 페이지
- 백업·복구 방법 문서화
- 비용과 API 호출량 모니터링

산출물: 개인 서버나 클라우드에서 안정적으로 운영 가능한 버전.

## 18. 기능별 완료 기준

새 기능은 다음 조건을 모두 만족할 때 완료로 간주한다.

- 정상 입력과 실패 입력을 처리한다.
- 네트워크 오류가 발생해도 기존 데이터가 보존된다.
- 화면에 로딩·빈 상태·오류 상태가 구분되어 나타난다.
- 핵심 로직에 자동 테스트가 있다.
- 사용자가 다음 행동을 알 수 있는 메시지가 있다.
- README 또는 이 문서에 사용법과 제한사항이 반영된다.

## 19. Codex에 전달할 첫 구현 요청문

다음 문장을 이 문서와 함께 Codex에 제공한다.

```text
PROJECT_PLAN.md를 프로젝트의 제품·기술 기준 문서로 사용해 뉴스·주식 레이더의 MVP를 구현해줘.

먼저 현재 폴더의 파일과 실행 환경을 점검하고, 기존 코드가 있으면 보존하면서 변경 계획을 제시해줘. 1차 구현 범위는 관심 종목 설정, 주가 조회, RSS 뉴스 조회, SQLite 저장, Streamlit 대시보드야. 자동 주문, 투자 추천, 유료 API 의존성은 추가하지 마.

구현 전후로 이 문서의 불변조건을 확인하고, 네트워크 실패·빈 결과·중복 뉴스·잘못된 종목 코드도 처리해줘. 완료 후 실행 방법, 변경 파일, 테스트 방법, 테스트 결과, 외부 API의 한계를 간단히 보고해줘.
```

## 20. 사용자에게 먼저 확인할 선택사항

구현자는 아래 값을 합리적인 기본값으로 시작할 수 있지만, 프로젝트 시작 시 사용자에게 확인하면 개인화가 쉬워진다.

- 초기 시장: 한국 / 미국 / 둘 다
- 초기 관심 종목: 5~10개
- 화면 언어: 한국어
- 기본 뉴스 범위: 경제·기업·AI·개발
- 수집 주기: 30분
- 알림 채널: 없음 또는 Telegram
- 저장 기간: 기본 90일

기본값으로 시작한 뒤에도 설정 화면에서 변경할 수 있게 만드는 것을 권장한다.
